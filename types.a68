
MODE POS = STRUCT(
  INT x, y
);

MODE TOKEN = STRUCT(
  INT kind,
  STRING literal
);

MODE RES = STRUCT(
  INT status,
  STRING source
);

MODE THEME = STRUCT(
  STRING
    assign,
    comment,
    string,
    number,
    brackets,
    important,
    keyword,
    unknown,
    function
)
